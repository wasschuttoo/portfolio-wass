import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Experience',
  description: 'Explore Wassil Chuttoo\'s professional experience — from Associate Test Engineer to Senior SDET at Dayforce, with progressive responsibilities in automation architecture and team leadership.',
};

export default function ExperienceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
